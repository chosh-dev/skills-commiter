interface:
  display_name: "Commiter"
  short_description: "Plan semantic commits from mixed git diffs"
  default_prompt: "Use $commiter to split current git changes into semantic commits and apply them safely. Keep chat minimal: show the plan, ask once for cancel/normal/fast, then execute immediately for normal/fast with no extra intent narration or post-apply verification chatter unless requested."
