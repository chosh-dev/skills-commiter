interface:
  display_name: "Commiter"
  short_description: "Plan semantic commits from mixed git diffs"
  default_prompt: "Use $commiter to split current git changes into semantic commits and apply them safely. Execute analysis first with minimal chatter, then show only the commit plan and one final apply confirmation (normal/fast)."
